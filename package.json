{
  "name": "messenger",
  "private": true,
  "scripts": {
    "dev": "npm run db:start && concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "db:start": "node scripts/db.mjs start",
    "db:stop": "node scripts/db.mjs stop",
    "db:migrate": "npm run prisma:migrate --prefix server",
    "db:seed": "npm run prisma:seed --prefix server",
    "setup": "node scripts/setup.mjs",
    "render-build": "npm install --prefix server && npm run build --prefix server && npm prune --prefix server --omit=dev && npm install --include=dev --prefix client && npm run build --prefix client",
    "start": "cd server && node dist/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
